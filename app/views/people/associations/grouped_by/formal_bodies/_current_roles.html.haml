.block--border__bottom
  %h2= t('people.associations.current_roles')
  %ul.list--block
    - @history[:current].each do |role|
      = render '/people/associations/current_role', role: role, role_title: "#{t('member').capitalize} #{t('of_the')} #{role.formal_body.name}", role_type: t('people.associations.committee_role')
