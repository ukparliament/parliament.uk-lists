#content.section--primary{ tabindex: "0" }
  .container
    %h1
      %span= title("#{@person.display_name} - #{t('.title', current_and_former: t('current_and_former').capitalize, parties_and_groups: t('parties_and_groups'))}")

%section
  .container
    %ol.list--block
      = render partial: 'people/party_membership', collection: @party_memberships
