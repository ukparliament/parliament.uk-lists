- if Parliament::Utils::Helpers::FlagHelper.election? || Parliament::Utils::Helpers::FlagHelper.post_election?
  .status--highlight.theme--caution
    %p
      - if Parliament::Utils::Helpers::FlagHelper.election?
        - if @single_mp
          = t('shared.election_message.description_singular')
        - else
          = t('shared.election_message.description_text')
          = link_to(t('shared.dissolution_message.description_link'), postcodes_path)
      - else
        = t('shared.post_election_message.description_text')
