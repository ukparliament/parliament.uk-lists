#content.section--primary{ tabindex: "0" }
  .container
    %h1
      %span= title("#{t('current_and_former').capitalize} #{t('committee')} #{t('member_plural')} - #{@group.name}")
      %span= "#{t('a_to_z.a_to_z')} - #{t('a_to_z.select_letter')}"

    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :group_memberships_a_z_letter_path, route_args: [@group.graph_id], all_path: @all_path }
