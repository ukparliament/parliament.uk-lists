.section--primary
  .container
    %h1
      %span= title("#{t('current').capitalize} #{t('groups')}")
      %span.context= "#{t('a_to_z.a_to_z')} - #{t('a_to_z.showing_all')}"

    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :groups_current_a_z_letter_path }

%section#content{ tabindex: "0" }
  .container
    = render 'group_list', groups: @groups
