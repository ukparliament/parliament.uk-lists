#content.section--primary{ tabindex: "0" }
  .container
    %h1
      %span= title("#{t('current_and_former').capitalize} #{t('government_organisations')}")
      %span= "#{t('a_to_z.a_to_z')} - #{t('a_to_z.select_letter')}"

    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :groups_government_organisations_a_z_letter_path, all_path: @all_path }
