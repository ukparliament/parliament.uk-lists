- title("#{@place.name} #{'current'} #{t('constituency_plural')} #{t('a_to_z.a_to_z')} #{t('a_to_z.showing_results')} '#{params[:letter]}'")
.section--primary
  .container
    %h1
      %span= "#{@place.name} #{'current'} #{t('constituency_plural')}"
      %span= "#{t('a_to_z.a_to_z')} #{t('a_to_z.showing_results')} '#{params[:letter].capitalize}'"
    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :places_constituencies_current_a_z_letter_path, route_args: [@place.graph_id] }
